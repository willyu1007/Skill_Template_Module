---
name: manage-integration-scenarios
description: Maintain integration scenarios (SSOT) and run compile/validation/execution checks.
category: scaffold/integration
ssot_path: .ai/skills/scaffold/integration/manage-integration-scenarios
---

# manage-integration-scenarios (entry)

Canonical source: `.ai/skills/scaffold/integration/manage-integration-scenarios/`

Open `.ai/skills/scaffold/integration/manage-integration-scenarios/SKILL.md` and any supporting files referenced there (for example `reference.md`, `examples.md`, `scripts/`, `templates/`).

> **Note**: The frontmatter above is identical to the canonical source except for `ssot_path` and `category` which are added for navigation. After opening the source file, skip re-reading the description to avoid redundant token usage.
