---
name: manage-project-state
description: Maintain .ai/project/state.json (stage and non-secret config references) via projectctl for consistent automation gates.
category: scaffold/project-config
ssot_path: .ai/skills/scaffold/project-config/manage-project-state
---

# manage-project-state (entry)

Canonical source: `.ai/skills/scaffold/project-config/manage-project-state/`

Open `.ai/skills/scaffold/project-config/manage-project-state/SKILL.md` and any supporting files referenced there (for example `reference.md`, `examples.md`, `scripts/`, `templates/`).

> **Note**: The frontmatter above is identical to the canonical source except for `ssot_path` and `category` which are added for navigation. After opening the source file, skip re-reading the description to avoid redundant token usage.
