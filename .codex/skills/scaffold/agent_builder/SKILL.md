---
name: agent_builder
description: Scaffold a module-embedded agent component (agent proxy) and integrate its API/contract artifacts into module-first SSOT.
category: scaffold
ssot_path: .ai/skills/scaffold/agent_builder
---

# agent_builder (entry)

Canonical source: `.ai/skills/scaffold/agent_builder/`

Open `.ai/skills/scaffold/agent_builder/SKILL.md` and any supporting files referenced there (for example `reference.md`, `examples.md`, `scripts/`, `templates/`).

> **Note**: The frontmatter above is identical to the canonical source except for `ssot_path` and `category` which are added for navigation. After opening the source file, skip re-reading the description to avoid redundant token usage.
