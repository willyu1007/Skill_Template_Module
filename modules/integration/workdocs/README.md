# Integration workdocs

Use this directory for cross-module debugging and integration planning.

Recommended structure:

- `active/` — current integration work
- `archive/` — completed integration work

Integration scenarios live in `modules/integration/scenarios.yaml`.
