---
name: initialize-module-instance
description: Create a new module instance with modulectl and register it into the modular SSOT/derived registries.
---

# initialize-module-instance (entry)

Canonical source: `.ai/skills/scaffold/modules/initialize-module-instance/`

Open `.ai/skills/scaffold/modules/initialize-module-instance/SKILL.md` and any supporting files referenced there (for example `reference.md`, `examples.md`, `scripts/`, `templates/`).

> **Note**: The frontmatter above is identical to the canonical source. After opening the source file, skip re-reading the description to avoid redundant token usage.
