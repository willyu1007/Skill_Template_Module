---
name: update-workdocs-for-handoff
description: Finalize/synchronize a task’s workdocs for handoff or wrap-up in module/integration scope; docs only (no new code changes).
category: workflows/workdocs
ssot_path: .ai/skills/workflows/workdocs/update-workdocs-for-handoff
---

# update-workdocs-for-handoff (entry)

Canonical source: `.ai/skills/workflows/workdocs/update-workdocs-for-handoff/`

Open `.ai/skills/workflows/workdocs/update-workdocs-for-handoff/SKILL.md` and any supporting files referenced there (for example `reference.md`, `examples.md`, `scripts/`, `templates/`).

> **Note**: The frontmatter above is identical to the canonical source except for `ssot_path` and `category` which are added for navigation. After opening the source file, skip re-reading the description to avoid redundant token usage.
